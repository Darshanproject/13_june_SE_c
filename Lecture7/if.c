/*
1). Simple if
syntax:
		if(condtion){
		 		statement;
		}
2). if else
3). else if leadder
4). nested if
5). Switch 
*/
#include<stdio.h>
main()

{
	int age;
	printf("Enter your age here : ");
	scanf("%d",&age);
	if(age > 35)
	{
		printf("This is true");
	}
}
